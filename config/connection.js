// Imports the mongoose library
const mongoose = require("mongoose");

// Connects to the MongoDB database using the MongoDB URI provided in the environment variables or using the default URI if the environment variable isn't set
mongoose.connect